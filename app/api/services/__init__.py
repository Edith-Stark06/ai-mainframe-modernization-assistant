"""Small, pure glue modules shared by more than one API router.

Nothing here performs analysis, generation, compilation, or validation
itself -- each module only classifies/shapes results already produced
by the existing Phase 1-11 pipeline (see
``app.dataset.modernization_bundle``).
"""
