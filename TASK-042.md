# TASK-042 — Java Compilation Tests & Production Analysis Service

## Overview

Task 042 covers:
1. **Java Compilation Testing**: Automated tests that compile generated Java source with `javac`.
2. **Production Analysis Service**: Extracting COBOL compilation/analysis orchestration into `app/analysis/`.

---

# Part 1: Java Compilation Tests

## Objective

Implement automated tests that compile the Java source generated by the compiler to verify that it is syntactically valid Java.

---

## Background

The compiler currently validates:

- Unit behavior
- End-to-end translation
- Regression scenarios
- Golden-file output

This task adds one more level of confidence by ensuring the generated Java code can be successfully compiled with the Java compiler.

---

## Scope

Create a Java compilation test suite that:

```
COBOL Source
      ↓
Compiler Pipeline
      ↓
Generated Java
      ↓
javac
      ↓
Compilation Result
```

---

## Functional Requirements

### 1. Create Compilation Test Structure

Create:

```
tests/compilation/
```

---

### 2. Fixture Discovery

Reuse existing COBOL fixtures where practical (for example, from `tests/golden/` or `tests/integration/`) instead of duplicating them.

Automatically discover fixtures.

---

### 3. Generate Java

For each fixture:

1. Execute the complete compiler pipeline.
2. Generate Java source.
3. Write the source to a temporary directory.

Do not modify repository files.

---

### 4. Compile Generated Java

Invoke the Java compiler (`javac`) on the generated source.

If Java is unavailable, skip the test with a clear pytest skip message rather than failing.

---

### 5. Assertions

For successful fixtures:

- Java compilation succeeds.
- Exit code is zero.
- No compiler errors.

For invalid COBOL fixtures:

- The compiler should stop before Java generation.
- `javac` should not be invoked.

---

### 6. Temporary Files

Use pytest temporary directories (`tmp_path` or equivalent).

Clean up automatically after execution.

---

### 7. Failure Reporting

On compilation failure, report:

- fixture name
- generated Java file path
- javac stderr
- javac stdout (if present)

---

### 8. Deterministic Execution

Running the tests multiple times should produce identical compilation results.

---

## Testing

Run:

```bash
black --check .
ruff check .
mypy app
pytest
pytest tests/compilation -v
```

---

## Documentation

Update:

```
docs/testing.md
```

Document:

- Java compilation testing
- Java requirement
- skipped-test behavior
- troubleshooting compilation failures

---

## Acceptance Criteria

- `tests/compilation/` created
- automatic fixture discovery
- generated Java compiled with `javac`
- invalid fixtures handled correctly
- temporary directories used
- clear failure reporting
- documentation updated
- Black passes
- Ruff passes
- MyPy passes
- Full pytest suite passes

---

## Non-goals

Do **not** implement:

- Runtime execution of generated Java
- Performance benchmarking
- Maven/Gradle project generation

---

## Branch

```
feat/task-042
```

---

## Files to Add

```
tests/compilation/
docs/testing.md
```

---

## Deliverables

- Java compilation test framework
- Automatic fixture discovery
- Temporary compilation workspace
- `javac` integration
- Updated testing documentation

---

# Part 2: Production Analysis Service

## Objective

Extract the existing COBOL compilation/analysis orchestration from
`tests/integration/helpers.py` into production code under `app/analysis/`.

The goal is to create one reusable production service that the future
FastAPI API layer can call.

This task is an architectural extraction only.

**Do not change the behavior of the lexer, parser, semantic analyzer,
IR builder, or Java generator.**

---

## Existing Pipeline

The existing integration pipeline is:

COBOL Source
    ↓
CobolLexer
    ↓
ProgramParser
    ↓
SemanticAnalyzer
    ↓
IRBuilder
    ↓
Java Backend
    ↓
Java Source + Diagnostics

The production service must preserve this exact order.

---

## Files to Create

Create:

```text
app/
└── analysis/
    ├── __init__.py
    ├── models.py
    └── service.py
```
